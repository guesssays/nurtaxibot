[build]
  command = "npm run build"
  functions = "netlify/functions"
  publish = "public"

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["@prisma/client", "xlsx"]
  included_files = ["node_modules/.prisma/client/**"]

[dev]
  framework = "#static"
